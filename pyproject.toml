[build-system]
requires = ["setuptools>=77.0,<83"]
build-backend = "setuptools.build_meta"

[project]
name = "totalrecall"
version = "0.2.0"
description = "Recursive memory compression system with LLM distillation — single DB backend"
requires-python = ">=3.11,<3.15"
dependencies = [
    "openai==2.24.0",
]

[project.scripts]
totalrecall = "totalrecall.cli:main"

[tool.setuptools.packages.find]
include = ["totalrecall*"]

[tool.uv]
package = true
